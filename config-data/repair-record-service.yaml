server:
  port: 8091
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tingesoLAB2
    username: postgres
    password: 1234
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
  instance:
    hostname: localhost
    #instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
