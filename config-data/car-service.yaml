server:
  port: ${PORT:${SERVER_PORT:0}}
spring:
  datasource:
    url: jdbc:postgresql://car-service-db:5432/car-service-db
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://eureka-service-service:8761/eureka/
  instance:
    preferIpAddress: true
